import React from "react";
import LoginButton from "./LoginButton";
import { Layout } from "./Layout";

const Home = () => {
  return (
    <Layout>
      <LoginButton />
    </Layout>
  );
};

export default Home;
